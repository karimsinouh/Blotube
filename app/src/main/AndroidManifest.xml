<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blotube">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blotube">

        <activity
            android:name=".ui.showPost.ShowPostActivity"
            android:label="@string/title_activity_show_post" />

        <activity
            android:name=".ui.playlistInfo.PlaylistInfoActivity"
            android:label="huhuhu"/>

        <activity
            android:name=".ui.videoInfo.VideoInfoActivity"
            android:label="@string/title_activity_video_info" />

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>